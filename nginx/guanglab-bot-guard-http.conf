# HTTP-level guards — installed to /etc/nginx/conf.d/
# Rate limit aggressive POST sweeps on static sites (e.g. fs.guanglab.org/posts/)

limit_req_zone $binary_remote_addr zone=guanglab_static_posts:10m rate=30r/m;
limit_req_status 429;
